<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ivf_dj">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name=".IVF_DJ"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".feature.edit.staff.ManageStaffActivity"></activity>
        <activity android:name=".feature.lecture.list.addlecture.AddLectureActivity" />
        <activity android:name=".feature.edit.SettingActivity" />
        <activity android:name=".feature.timeTable.TimeTableActivity" />
        <activity android:name=".feature.notification.NotificationActivity" />
        <activity android:name=".feature.lecture.list.LectureListActivity" />
        <activity android:name=".feature.lecture.mylecture.MyLectureActivity" />
        <activity android:name=".feature.main.MainActivity" />
        <activity android:name=".feature.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>